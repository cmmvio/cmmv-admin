{
    "$schema": "https://turbo.build/schema.json",
    "globalDependencies": ["packages/client/**", "packages/dashboard/**"],
    "tasks": {
        "dev": {
            "cache": false,
            "persistent": true
        },
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["dist/**"]
        }
    }
}
